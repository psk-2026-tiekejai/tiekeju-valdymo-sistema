services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: tiekejai_db
      POSTGRES_USER: tiekejai
      POSTGRES_PASSWORD: tiekejai_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
